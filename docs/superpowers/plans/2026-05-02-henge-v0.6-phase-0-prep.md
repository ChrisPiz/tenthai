# Henge v0.6 — Phase 0: Preparación del repo

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Branch limpio, deps actualizadas, `.env.example` con OPENAI_API_KEY documentada, tests baseline en verde. No tocar lógica.

**Architecture:** Cambios solo en metadata del repo y configuración. Cero impacto runtime. Punto de retorno seguro vía tag.

**Tech Stack:** git, pip, pytest.

---

## File Structure

- Modify: `requirements.txt` — confirmar `openai>=1.50.0` (ya presente, validar)
- Modify: `.env.example` — añadir feature flags v0.6 + locale + EMBED_PROVIDER/MODEL
- Modify: `pyproject.toml:6` — bump version `0.5.0` → `0.6.0-dev`
- Create: `docs/superpowers/plans/.gitkeep` (si vacío) — N/A, ya hay archivos

Tests baseline existentes (`tests/test_critical.py`, `tests/test_smoke.py`, `tests/conftest.py`) NO se modifican en esta fase.

---

### Task 0.1: Branch y baseline tag

**Files:**
- N/A (operaciones git)

- [ ] **Step 1: Verificar working tree limpio**

```bash
cd /Users/futhart/Dropbox/Studiolab/services/Henge
git status --short
```

Expected: solo `M docs/demo.html` y `M henge/viz.py` (snapshot inicial). Si hay otros, commitear o stash antes.

- [ ] **Step 2: Stash cambios pendientes para no contaminar branch**

```bash
git stash push -m "pre-v0.6-stash" -- docs/demo.html henge/viz.py
```

Expected: stash creado, working tree limpio.

- [ ] **Step 3: Crear branch y tag de retorno**

```bash
git checkout -b v0.6-multi-model
git tag pre-v0.6-baseline
```

Expected: en branch `v0.6-multi-model`, tag `pre-v0.6-baseline` apunta al último commit de main.

- [ ] **Step 4: Verificar**

```bash
git branch --show-current
git tag --list | grep pre-v0.6-baseline
```

Expected: `v0.6-multi-model` y `pre-v0.6-baseline` listed.

---

### Task 0.2: Validar tests baseline en verde

**Files:**
- N/A (solo ejecuta suite existente)

- [ ] **Step 1: Instalar deps en venv**

```bash
source .venv/bin/activate
pip install -r requirements.txt -r requirements-dev.txt
```

Expected: instalación sin error. Si `openai>=1.50.0` ya satisfecho, pip lo reporta.

- [ ] **Step 2: Correr suite completa**

```bash
pytest -q
```

Expected: todos los tests pasan. Si alguno falla, **NO avanzar**. Arreglar primero en commit aparte (regla del spec: no mezclar fixes con cambios de lógica v0.6).

- [ ] **Step 3: Commit (ninguno; baseline solo verificado)**

No hay cambios. Solo confirmación de que el branch arranca verde.

---

### Task 0.3: Actualizar `.env.example` con vars v0.6

**Files:**
- Modify: `/Users/futhart/Dropbox/Studiolab/services/Henge/.env.example`

- [ ] **Step 1: Reemplazar contenido**

Sobrescribir `.env.example` para incluir feature flags y embed config:

```env
# Required: Anthropic API key for Claude Haiku, Sonnet, Opus.
# Get one at https://console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-...

# Required (v0.6+): OpenAI API key for GPT-5 frames + cross-lab audit layer.
# Get one at https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# Advanced opt-in: Voyage embeddings (higher Spanish quality).
# WARNING: Voyage's free tier (3 RPM / 10K TPM, no payment method) cannot
# complete a single Henge run. Add a payment method on
# https://dashboard.voyageai.com/ first (200M free tokens/month still apply).
# VOYAGE_API_KEY=pa-...

# Embeddings
EMBED_PROVIDER=openai          # openai | voyage
EMBED_MODEL=text-embedding-3-large

# Locale: report chrome language. Auto-detects from question if unset.
HENGE_LOCALE=auto              # auto | en | es

# Feature flags (v0.6+). Default true; set false to skip the layer.
HENGE_ENABLE_ADVERSARIAL_SCOPING=true
HENGE_ENABLE_META_FRAME=true
HENGE_ENABLE_TENTH_MAN_DUAL=true
HENGE_ENABLE_CLAIM_VERIFICATION=true
```

- [ ] **Step 2: Verificar diff**

```bash
git diff .env.example
```

Expected: nuevas vars presentes, sin tokens reales filtrados.

- [ ] **Step 3: Commit**

```bash
git add .env.example
git commit -m "chore(v0.6): document OpenAI key + feature flags in .env.example"
```

---

### Task 0.4: Validar `requirements.txt` y `pyproject.toml`

**Files:**
- Modify: `/Users/futhart/Dropbox/Studiolab/services/Henge/pyproject.toml:6` (version bump)

- [ ] **Step 1: Confirmar `openai>=1.50.0` ya presente**

```bash
grep -n "openai" requirements.txt pyproject.toml
```

Expected: `openai>=1.50.0` en ambos. (Repo ya lo tiene; no acción si correcto.)

- [ ] **Step 2: Bump version a `0.6.0-dev`**

Editar `pyproject.toml`. Cambiar `version = "0.5.0"` → `version = "0.6.0-dev"`.

- [ ] **Step 3: Verificar build metadata**

```bash
python -c "import tomllib; print(tomllib.load(open('pyproject.toml','rb'))['project']['version'])"
```

Expected: `0.6.0-dev`.

- [ ] **Step 4: Commit**

```bash
git add pyproject.toml
git commit -m "chore(v0.6): bump version to 0.6.0-dev"
```

---

### Task 0.5: Re-ejecutar suite tras cambios

**Files:**
- N/A

- [ ] **Step 1: Tests siguen verdes con metadata cambiada**

```bash
pytest -q
```

Expected: PASS. Si falla algún test que lee version o env, fix y commit aparte.

- [ ] **Step 2: Smoke import**

```bash
python -c "import henge; from henge import server, agents, embed, pricing, scoping, consensus, storage, viz, updater; print('imports ok')"
```

Expected: `imports ok`.

- [ ] **Step 3: Commit (ninguno; solo verificación)**

No cambios.

---

## Closure criterion (Phase 0)

- Branch `v0.6-multi-model` creado, tag `pre-v0.6-baseline` apunta al merge-base.
- `pytest -q` pasa al final de la fase.
- `.env.example` documenta `OPENAI_API_KEY`, `EMBED_PROVIDER`, `EMBED_MODEL`, `HENGE_LOCALE`, y los 4 feature flags.
- `pyproject.toml` reporta `0.6.0-dev`.
- 2 commits atómicos en el branch (env example, version bump).

Si falla algo, NO avanzar a Phase 1. Spec regla #1: "No saltarse fases."
